function circleArea(r) {
  const pi = 3.14;
  console.log(pi * r * r);
}

function circleCircumference(r) {
  const pi = 3.14;
  console.log(2 * pi * r);
}

module.exports = {
  circleArea,
  circleCircumference,
};
